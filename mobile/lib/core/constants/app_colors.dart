import 'package:flutter/material.dart';

abstract final class AppColors {
  // Primary
  static const primary = Color(0xFF1B4332);
  static const primaryLight = Color(0xFF2D6A4F);
  static const primaryDark = Color(0xFF0F2B1F);

  // Secondary
  static const secondary = Color(0xFFD4A843);
  static const secondaryLight = Color(0xFFE8C976);
  static const secondaryDark = Color(0xFFB08930);

  // Accent
  static const accent = Color(0xFF4DA8DA);

  // Neutrals
  static const background = Color(0xFFFAFBFC);
  static const surface = Color(0xFFF0F2F5);
  static const surfaceWhite = Color(0xFFFFFFFF);
  static const textPrimary = Color(0xFF1A1A2E);
  static const textSecondary = Color(0xFF6B7280);
  static const textTertiary = Color(0xFF9CA3AF);
  static const border = Color(0xFFE5E7EB);

  // Semantic
  static const error = Color(0xFFDC2626);
  static const success = Color(0xFF059669);
  static const warning = Color(0xFFF59E0B);
  static const info = Color(0xFF3B82F6);

  // SOS
  static const sos = Color(0xFFDC2626);
  static const sosBackground = Color(0xFFFEE2E2);
}
